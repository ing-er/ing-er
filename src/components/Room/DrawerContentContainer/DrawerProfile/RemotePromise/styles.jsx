import styled from 'styled-components';

const Wrapper = styled.div`
  & .MuiInputBase-input {
    color: black;
  }
  & textarea {
    text-align: center;
    font-weight: bold;
  }
`

export default Wrapper;